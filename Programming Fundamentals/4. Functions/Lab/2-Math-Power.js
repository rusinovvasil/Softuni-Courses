function power(number, power) {

    return number ** power;

    // let calculatedSum = number ** power;
    // console.log(calculatedSum);
}

// Don't copy the calling of the function in judge, just the code above
power(2, 8);
