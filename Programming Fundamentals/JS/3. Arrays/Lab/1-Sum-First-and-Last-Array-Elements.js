function sumAndElements(numbers) {
    let firstElement = numbers[0];
    let lastElement = numbers[numbers.length - 1]; //взима последния елемент от масива

    let sum = firstElement + lastElement;
    
    console.log(sum)
}
sumAndElements[20, 30, 40]